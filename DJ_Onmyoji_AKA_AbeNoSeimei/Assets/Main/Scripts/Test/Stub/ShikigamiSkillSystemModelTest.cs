using System.Collections;
using System.Collections.Generic;
using Main.Model;
using UnityEngine;

namespace Main.Test.Stub
{
    public class ShikigamiSkillSystemModelTest : MonoBehaviour
    {
        [SerializeField] private ShikigamiInfo[] shikigamis;
        public ShikigamiInfo[] Shikigamis => shikigamis;
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
