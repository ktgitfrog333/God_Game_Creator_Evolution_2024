using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Main.View
{
    /// <summary>
    /// 基本UIの説明（魂(経験値)）用UI
    /// ビュー
    /// </summary>
    public class GuideUITheClearRewardTextContentsView : GuideUITheUniversalView
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
